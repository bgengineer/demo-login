import mongoose from "mongoose";

const regSchema = mongoose.Schema({
    name: {
        type: String,
        required: true
    },
    email: {
        type: String,
        required: true
    },
    password: {
        type: String,
        required: true
    }
})

const registerModel = mongoose.model('registerModel', regSchema)

export default registerModel